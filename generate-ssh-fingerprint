#!/bin/bash

PATH="/usr/bin:/usr/sbin"

[ -n "$1" ] || exit 1

set -o errexit	# set -e
set -o nounset	# set -u

renice --priority 19 --pid $$ > /dev/null
ionice --class 3     --pid $$ > /dev/null

cd /var/named

ssh-keygen -r @ > ssh-fingerprint.db

